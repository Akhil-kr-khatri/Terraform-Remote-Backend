# Terraform working directory
.terraform/
**/.terraform/

# Terraform state files (NEVER commit)
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.backup

# Terraform dependency lock file (optional to commit; ignored here)
.terraform.lock.hcl

# Shell scripts with secrets
startUpFile.sh

# Editor / OS files
.vscode/
.idea/
.DS_Store
